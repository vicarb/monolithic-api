// UserValidation.ts
import { IValidation } from "../interfaces/Validation/IValidation";

export class UserValidation implements IValidation {
  validate(data: any): void {
    // Implementation for validating user input data
    // e.g., check email format, required fields, etc.
  }
}
