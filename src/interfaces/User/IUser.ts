// src/interfaces/User/IUser.ts

export interface IUser {
    id: string;
    email: string;
    password: string;
    username?: string;
    // Other common properties like first name, last name, etc.
  }
  